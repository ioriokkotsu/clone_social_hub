name: clone_social_hub
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  # UI-related packages from the original project
  cupertino_icons: ^1.0.8
  google_fonts: ^8.1.0
  fan_floating_menu: ^0.0.4
  shimmer: ^3.0.0
  intl: ^0.20.2
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
